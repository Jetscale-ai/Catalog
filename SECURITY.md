# Security Policy

## Supply Chain
- This repository defines *where* code comes from. Ensure `repoURL` fields point to trusted registries (GHCR).
- **No Plugins:** Blueprints must be consumable by standard ArgoCD without custom plugins (CMP/AVP).
